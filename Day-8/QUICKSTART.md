# 🚀 Quick Start Guide

## 빠른 시작 (5분 안에 시작하기)

### 1단계: 환경 설정

```bash
# new_day-8 디렉토리로 이동
cd ~/workspace/samsung_2025/samsung_edu/new_day-8

# Conda 환경 생성 (최초 1회만)
conda env create -f environment.yml

# 환경 활성화
conda activate day8-ml
```

### 2단계: Jupyter 실행

```bash
# Jupyter Notebook 실행
jupyter notebook
```

브라우저가 자동으로 열리면 `notebooks/` 폴더로 이동하세요.

### 3단계: 노트북 실행 순서

1. **1-Logistic_Regression_SECOM.ipynb** (60분)
   - SECOM 반도체 데이터로 불량 예측
   - 클래스 불균형 처리 학습

2. **2-NaiveBayes_Text.ipynb** (45분)
   - 텍스트 뉴스 기사 분류
   - TF-IDF vectorization

3. **3-KNN_Classic.ipynb** (45분)
   - Iris, Wine 데이터로 기본 이해
   - k 값 선택, 차원의 저주

4. **4-SVM_SECOM.ipynb** (60분)
   - SECOM 데이터 재방문
   - Linear vs RBF kernel 비교

---

## 문제 해결

### Q: Conda 명령어를 찾을 수 없어요
```bash
# .bashrc에 conda alias 확인
source ~/.bashrc
```

### Q: Jupyter에서 커널을 찾을 수 없어요
```bash
# 커널 등록
python -m ipykernel install --user --name day8-ml --display-name "Python 3.9 (Day8-ML)"
```

### Q: 데이터를 찾을 수 없다는 에러가 나요
```bash
# 데이터 파일 확인
ls -lh data/

# 노트북에서 경로 확인 (상대 경로 ../data/ 사용)
```

### Q: 메모리 부족 에러
- GridSearchCV의 n_jobs를 줄이세요 (예: n_jobs=2)
- 샘플 데이터로 먼저 테스트하세요

---

## 예상 실행 시간

| 노트북 | 실행 시간 | GridSearchCV 포함 |
|--------|-----------|-------------------|
| 1-Logistic_Regression | ~5분 | ~8분 |
| 2-NaiveBayes | ~3분 | ~5분 |
| 3-KNN | ~4분 | ~7분 |
| 4-SVM | ~3분 | ~10분 |

**전체 실행**: 약 30분 (GridSearchCV 포함 시)

---

## 학습 팁

1. **셀 단위 실행**: 각 셀을 이해하면서 천천히 실행
2. **시각화 확인**: 그래프가 의미하는 바를 생각하며 진행
3. **파라미터 변경**: k 값, C 값 등을 바꿔보며 실험
4. **에러 분석**: 에러 메시지를 읽고 이해하려고 노력

---

**즐거운 학습 되세요! 🎓**
